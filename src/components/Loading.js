import React from "react";
import "../styles/Loading.css";

const Loading = () => {
    return(
        <div className="loading">
            <p>Loading...</p>
        </div>
    )
};

export default Loading;
